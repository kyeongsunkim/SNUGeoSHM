# Dash Callback Flow Diagrams

## Visual Guide to Understanding Callbacks

---

## 📊 Basic Callback Flow

```
┌─────────────┐
│   Browser   │
│   (User)    │
└──────┬──────┘
       │ 1. User clicks button
       ▼
┌─────────────────────────────┐
│   Component Property        │
│   button.n_clicks = 1       │
└──────┬──────────────────────┘
       │ 2. Property change detected
       ▼
┌─────────────────────────────┐
│   Dash Framework            │
│   Finds matching callback   │
└──────┬──────────────────────┘
       │ 3. Calls callback function
       ▼
┌─────────────────────────────┐
│   Python Function           │
│   def my_callback(n):       │
│       return process()      │
└──────┬──────────────────────┘
       │ 4. Returns new values
       ▼
┌─────────────────────────────┐
│   Output Components         │
│   Updates div.children      │
└──────┬──────────────────────┘
       │ 5. Send to browser
       ▼
┌─────────────┐
│   Browser   │
│  Re-renders │
└─────────────┘
```

---

## 🔄 Groundhog CPT Processing - Complete Flow

### User Journey: Upload → Process → Download

```
┌─────────────────────────────────────────────────────────────────────┐
│                          PAGE LOAD                                   │
└───────────────────────────────┬─────────────────────────────────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │   layout() renders    │
                    │   - Upload button     │
                    │   - Data table        │
                    │   - Process button    │
                    │   - Output div        │
                    └───────────┬───────────┘
                                │
                                ▼
┌───────────────────────────────────────────────────────────────────────┐
│                    STEP 1: USER UPLOADS FILE                          │
└───────────────────────────────┬───────────────────────────────────────┘
                                │
                                ▼
        ┌───────────────────────────────────────────┐
        │  User selects excel_example_cpt.xlsx      │
        └────────────────┬──────────────────────────┘
                         │
                         ▼
        ┌───────────────────────────────────────────────────┐
        │  dcc.Upload component                             │
        │  - Reads file                                     │
        │  - Base64 encodes                                 │
        │  - Sets upload-groundhog-cpt.contents            │
        └────────────────┬──────────────────────────────────┘
                         │ contents property CHANGED
                         ▼
        ┌─────────────────────────────────────────────────────────┐
        │  CALLBACK TRIGGERED:                                    │
        │  handle_groundhog_cpt_upload()                          │
        │                                                          │
        │  Input: upload-groundhog-cpt.contents  ← TRIGGER        │
        │  State: upload-groundhog-cpt.filename                   │
        │  State: global-store.data                               │
        └────────────────┬────────────────────────────────────────┘
                         │
                         ▼
        ┌─────────────────────────────────────────────────────────┐
        │  Function executes:                                     │
        │                                                          │
        │  1. df = process_uploaded_file(contents, filename)      │
        │     - Decode base64                                     │
        │     - Read Excel                                        │
        │     - Validate columns                                  │
        │                                                          │
        │  2. store_update['groundhog_cpt'] = df.to_dict()        │
        │     - Save to global storage                            │
        │                                                          │
        │  3. columns = [{"name": i, "id": i} for i in df.cols]   │
        │     - Prepare table format                              │
        │                                                          │
        │  4. fig = px.line(df, x="z [m]", y="qc [MPa]")          │
        │     - Create preview graph                              │
        │                                                          │
        │  5. return (alert, store, data, cols, fig)              │
        └────────────────┬────────────────────────────────────────┘
                         │ Returns 5 values
                         ▼
        ┌─────────────────────────────────────────────────────────┐
        │  Outputs update:                                        │
        │                                                          │
        │  Output 1: upload-groundhog-cpt-status.children         │
        │            → ✓ "excel_example_cpt.xlsx uploaded"        │
        │                                                          │
        │  Output 2: global-store.data                            │
        │            → {'groundhog_cpt': [{...}, {...}, ...]}     │
        │                                                          │
        │  Output 3: groundhog-cpt-data-table.data                │
        │            → [{row1}, {row2}, ...]                      │
        │                                                          │
        │  Output 4: groundhog-cpt-data-table.columns             │
        │            → [{"name": "z [m]", "id": "z [m]"}, ...]    │
        │                                                          │
        │  Output 5: groundhog-cpt-data-viz.figure                │
        │            → Plotly figure object                       │
        └────────────────┬────────────────────────────────────────┘
                         │
                         ▼
        ┌─────────────────────────────────────────────────────────┐
        │  Browser re-renders:                                    │
        │  ✓ Green success message appears                        │
        │  ✓ Data table shows CPT data                            │
        │  ✓ Graph displays qc vs depth                           │
        └─────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────────┐
│              STEP 2: USER UPLOADS LAYERING FILE                       │
│              (Same process as Step 1)                                 │
│              → Stores in global-store['groundhog_layering']           │
└───────────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────────┐
│                  STEP 3: USER CLICKS "PROCESS CPT"                    │
└───────────────────────────────┬───────────────────────────────────────┘
                                │
                                ▼
        ┌───────────────────────────────────────────┐
        │  User clicks "Process CPT" button         │
        └────────────────┬──────────────────────────┘
                         │
                         ▼
        ┌─────────────────────────────────────────────────────────┐
        │  dbc.Button component                                   │
        │  - n_clicks increments (0 → 1)                          │
        └────────────────┬────────────────────────────────────────┘
                         │ n_clicks property CHANGED
                         ▼
        ┌─────────────────────────────────────────────────────────┐
        │  CALLBACK TRIGGERED:                                    │
        │  run_groundhog()                                        │
        │                                                          │
        │  Input: run-groundhog-btn.n_clicks  ← TRIGGER           │
        │  State: global-store.data           ← READ              │
        └────────────────┬────────────────────────────────────────┘
                         │
                         ▼
        ┌─────────────────────────────────────────────────────────┐
        │  Function executes:                                     │
        │                                                          │
        │  1. Retrieve data from storage:                         │
        │     cpt_df = pd.DataFrame(store_data['groundhog_cpt'])  │
        │     layering_df = pd.DataFrame(store_data[              │
        │                    'groundhog_layering'])               │
        │                                                          │
        │  2. Initialize Groundhog:                               │
        │     cpt = PCPTProcessing(title='CPT Process')           │
        │                                                          │
        │  3. Load data with FIXED parameters:                    │
        │     cpt.load_pandas(                                    │
        │         cpt_df,                                         │
        │         z_key='z [m]',                                  │
        │         qc_key='qc [MPa]',                              │
        │         fs_key='fs [MPa]',                              │
        │         u2_key='u [kPa]'  ← BUG FIX #1                  │
        │     )                                                   │
        │                                                          │
        │  4. Create soil profile:                                │
        │     profile = SoilProfile(layering_df)                  │
        │     # BUG FIX #3: Returns DataFrame directly            │
        │                                                          │
        │  5. Generate plot:                                      │
        │     cpt.plot_raw_pcpt()  ← BUG FIX #5 (no params)       │
        │     img_src = matplotlib_to_base64(plt.gcf())           │
        │                                                          │
        │  6. Store results:                                      │
        │     store_update['groundhog_cpt_processed'] =           │
        │         cpt.data.to_dict('records')                     │
        │                                                          │
        │  7. return (output_html, store_update)                  │
        └────────────────┬────────────────────────────────────────┘
                         │ Returns 2 values
                         ▼
        ┌─────────────────────────────────────────────────────────┐
        │  Outputs update:                                        │
        │                                                          │
        │  Output 1: groundhog-output.children                    │
        │            → [html.Img(src=img_src), summary, ...]      │
        │                                                          │
        │  Output 2: global-store.data                            │
        │            → {..., 'groundhog_cpt_processed': [...]}    │
        └────────────────┬────────────────────────────────────────┘
                         │
                         ▼
        ┌─────────────────────────────────────────────────────────┐
        │  Browser re-renders:                                    │
        │  ✓ CPT profile plot appears                             │
        │  ✓ Processing summary displayed                         │
        │  ✓ Download button becomes active                       │
        └─────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────────┐
│              STEP 4: USER CLICKS "DOWNLOAD RESULTS"                   │
└───────────────────────────────┬───────────────────────────────────────┘
                                │
                                ▼
        ┌─────────────────────────────────────────────────────────┐
        │  CALLBACK TRIGGERED:                                    │
        │  download_groundhog()                                   │
        │                                                          │
        │  Input: download-groundhog-btn.n_clicks                 │
        │  State: global-store.data                               │
        └────────────────┬────────────────────────────────────────┘
                         │
                         ▼
        ┌─────────────────────────────────────────────────────────┐
        │  Function executes:                                     │
        │                                                          │
        │  1. df = pd.DataFrame(store_data[                       │
        │          'groundhog_cpt_processed'])                    │
        │                                                          │
        │  2. return dcc.send_data_frame(                         │
        │         df.to_csv,                                      │
        │         'groundhog_results.csv'                         │
        │     )                                                   │
        └────────────────┬────────────────────────────────────────┘
                         │
                         ▼
        ┌─────────────────────────────────────────────────────────┐
        │  Output: download-component.data                        │
        │  → Triggers browser download dialog                     │
        │  → User saves groundhog_results.csv                     │
        └─────────────────────────────────────────────────────────┘
```

---

## 🔗 Callback Chain Example

When multiple callbacks are connected:

```
User Action
    ↓
┌─────────────────────┐
│  Callback 1         │
│  Input: button      │
│  Output: store      │  ──────┐
└─────────────────────┘        │
                               │ store.data changes
                               ↓
                    ┌─────────────────────┐
                    │  Callback 2         │
                    │  Input: store       │  ← Triggered by Callback 1
                    │  Output: graph      │  ──────┐
                    └─────────────────────┘        │
                                                   │ graph.figure changes
                                                   ↓
                                        ┌─────────────────────┐
                                        │  Callback 3         │
                                        │  Input: graph       │  ← Triggered by Callback 2
                                        │  Output: summary    │
                                        └─────────────────────┘
```

**Example in code:**

```python
# Callback 1: Button → Store
@callback(
    Output('data-store', 'data'),
    Input('load-button', 'n_clicks')
)
def load_data(n):
    return {'loaded': True, 'value': 42}

# Callback 2: Store → Graph (triggered by Callback 1)
@callback(
    Output('my-graph', 'figure'),
    Input('data-store', 'data')  # ← Watches store
)
def update_graph(data):
    return px.bar(x=[1,2,3], y=[data['value'], 10, 20])

# Callback 3: Graph → Summary (triggered by Callback 2)
@callback(
    Output('summary', 'children'),
    Input('my-graph', 'figure')  # ← Watches graph
)
def show_summary(fig):
    return f"Graph has {len(fig['data'])} traces"
```

---

## 🎯 Input vs State Comparison

```
┌──────────────────────────────────────────────────────────────────┐
│                      INPUT vs STATE                              │
├─────────────────────────────┬────────────────────────────────────┤
│         INPUT               │            STATE                   │
├─────────────────────────────┼────────────────────────────────────┤
│ Triggers callback           │ Does NOT trigger callback          │
│                             │                                    │
│ @callback(                  │ @callback(                         │
│   Output(...),              │   Output(...),                     │
│   Input('btn', 'n_clicks')  │   Input('trigger', 'value'),       │
│ )                           │   State('btn', 'n_clicks')         │
│ def my_func(n_clicks):      │ )                                  │
│   # Runs when button        │ def my_func(trigger, n_clicks):    │
│   # is clicked              │   # Runs when 'trigger' changes    │
│   ...                       │   # Reads n_clicks but doesn't     │
│                             │   # trigger on button click        │
├─────────────────────────────┼────────────────────────────────────┤
│ Use for:                    │ Use for:                           │
│ - Buttons                   │ - Reading input values             │
│ - File uploads              │ - Current settings                 │
│ - Form submissions          │ - Global storage data              │
│ - Auto-refresh timers       │ - Configuration values             │
└─────────────────────────────┴────────────────────────────────────┘
```

**Visual Example:**

```
Scenario: User uploads file then clicks process

┌─────────────────────────────────────────────────────────────────┐
│  Upload Component                                               │
│  - contents: "base64data..."  ← Input in upload callback       │
│  - filename: "data.xlsx"      ← State in upload callback       │
└─────────────────────────────────────────────────────────────────┘
         │
         │ contents changes → callback 1 runs
         ↓
┌─────────────────────────────────────────────────────────────────┐
│  Global Store                                                   │
│  data: {'uploaded_file': [...]}  ← Output of callback 1        │
└─────────────────────────────────────────────────────────────────┘
                                          │
                                          │ Used as State in callback 2
                                          │ (doesn't trigger callback 2)
                                          ↓
┌─────────────────────────────────────────────────────────────────┐
│  Process Button                                                 │
│  n_clicks: 1  ← Input in callback 2 (triggers it!)             │
└─────────────────────────────────────────────────────────────────┘
         │
         │ n_clicks changes → callback 2 runs
         ↓
┌─────────────────────────────────────────────────────────────────┐
│  Callback 2 reads:                                              │
│  - n_clicks from Input (what triggered it)                      │
│  - store data from State (what it needs but didn't trigger)     │
│                                                                 │
│  def process(n_clicks, store_data):                             │
│      data = store_data['uploaded_file']  ← From State           │
│      # Process data...                                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Global Store Pattern

```
                    ┌─────────────────────┐
                    │   global-store      │
                    │   (Browser Memory)  │
                    └──────────┬──────────┘
                               │
            ┌──────────────────┼──────────────────┐
            │                  │                  │
            ▼                  ▼                  ▼
    ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
    │  Callback 1  │  │  Callback 2  │  │  Callback 3  │
    │              │  │              │  │              │
    │  Upload →    │  │  Process →   │  │  Download →  │
    │  WRITE store │  │  READ store  │  │  READ store  │
    └──────────────┘  └──────────────┘  └──────────────┘
```

**Data Structure:**

```python
global-store.data = {
    'groundhog_cpt': [      # ← Written by upload callback
        {'z [m]': 0.02, 'qc [MPa]': 0.00, ...},
        {'z [m]': 0.04, 'qc [MPa]': 0.00, ...},
        # ... 1000 rows
    ],
    'groundhog_layering': [  # ← Written by upload callback
        {'Depth from [m]': 0.0, 'Soil type': 'SAND', ...},
        # ... 5 layers
    ],
    'groundhog_cpt_processed': [  # ← Written by process callback
        # ... processed results
    ]
}
```

**Access Pattern:**

```python
# Writing
@callback(
    Output('global-store', 'data'),
    ...
)
def write_to_store(...):
    return {'my_key': my_value}

# Reading
@callback(
    ...,
    State('global-store', 'data')
)
def read_from_store(..., store_data):
    my_value = store_data.get('my_key')
    # Use my_value...

# Updating (preserve existing data)
@callback(
    Output('global-store', 'data', allow_duplicate=True),
    ...,
    State('global-store', 'data')
)
def update_store(..., store_data):
    store_update = store_data.copy()  # ← Keep existing
    store_update['new_key'] = new_value  # ← Add new
    return store_update
```

---

## 📈 Component Lifecycle

```
Page Load
    │
    ├─→ layout() function called
    │       │
    │       ├─→ Returns components
    │       │       │
    │       │       └─→ Components have initial values
    │       │
    │       └─→ Browser renders components
    │
    ├─→ Callbacks registered (but not run if prevent_initial_call=True)
    │
    └─→ Page ready for interaction

User Interaction
    │
    ├─→ Component property changes
    │       │
    │       └─→ Triggers callbacks watching that property (Input)
    │               │
    │               ├─→ Callback function executes
    │               │       │
    │               │       └─→ Returns new values
    │               │
    │               └─→ Output components update
    │                       │
    │                       └─→ Browser re-renders changed parts
    │
    └─→ May trigger more callbacks (cascade)
```

---

## 🎨 Multiple Inputs Example

```
┌────────────┐
│  Input 1   │ ────┐
└────────────┘     │
                   │
┌────────────┐     │     ┌──────────────┐
│  Input 2   │ ────┼────→│  Callback    │
└────────────┘     │     │  (OR logic)  │
                   │     └──────┬───────┘
┌────────────┐     │            │
│  Input 3   │ ────┘            │
└────────────┘                  ▼
                        ┌──────────────┐
                        │   Output     │
                        └──────────────┘

If ANY input changes → callback runs
```

**Code:**

```python
@callback(
    Output('result', 'children'),
    [
        Input('input1', 'value'),
        Input('input2', 'value'),
        Input('input3', 'value')
    ]
)
def combine(val1, val2, val3):
    # Runs when input1 OR input2 OR input3 changes
    return f"{val1} + {val2} + {val3} = {val1+val2+val3}"
```

---

## 🎯 Summary Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    DASH APP FLOW                                │
└─────────────────────────────────────────────────────────────────┘

1. APP INITIALIZATION
   ├─ Create Dash instance
   ├─ Define layout (components)
   ├─ Register callbacks
   └─ Start Flask server

2. PAGE LOAD
   ├─ Browser requests page
   ├─ Server sends HTML + JavaScript
   ├─ Browser renders components
   └─ Callbacks ready (not run if prevent_initial_call)

3. USER INTERACTION
   ├─ User acts (click, type, upload)
   ├─ Component property changes
   ├─ Browser sends change to server
   └─ Server finds matching callback

4. CALLBACK EXECUTION
   ├─ Extract Input values (triggers)
   ├─ Extract State values (context)
   ├─ Run Python function
   ├─ Return values for Outputs
   └─ Server sends updates to browser

5. UI UPDATE
   ├─ Browser receives new values
   ├─ Updates Output components
   ├─ Re-renders changed parts
   └─ May trigger more callbacks (cascade)

6. REPEAT 3-5
   └─ For each user interaction
```

---

**Generated:** 2025-12-01
**Purpose:** Visual guide to Dash callback mechanics
